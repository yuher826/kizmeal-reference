# MANIFEST — 키즈밀 식단표 정답본 자산

> **솔아에게** — 세션 시작 시 `kizmeal-renewal`의 `HANDOFF.md`와 함께 이 레포를 clone해서 이 파일을 읽는다. 코드 실측만으로 판단이 서지 않을 때 여기 실물이 정답본이다.
>
> `git clone https://github.com/yuher826/kizmeal-reference.git`
>
> public 레포라 `raw.githubusercontent.com`으로 개별 파일 접근도 가능하다. 기본 브랜치는 `main`이다 (`kizmeal-renewal`은 `master` — 헷갈리지 말 것).

## 무엇이 들어 있나 (2026-09-08 실측)

```
2026-07/   pptx 50 · pdf 49 · xlsx 2 · 계열 4폴더
2026-08/   pptx 57 · pdf 55 · xlsx 1 · 계열 4폴더
templates/ pptx 9   2026년 1~8월 디자인 양식
합계 235개 / 683MB
```

7·8월은 **자동화 이전 수작업 실배포본**이다. `weekly_menus` 테이블에는 행이 없다(6월·9월만 있음). 즉 시스템을 거치지 않은 순수 정답본이다.

## 가장 값나가는 것 — 입력·출력 쌍

두 달 모두 **영양사 실제 입력 엑셀**과 **그 결과 실배포본**이 함께 있다. 코드를 고친 뒤 "같은 엑셀을 넣으면 같은 결과가 나오는가"를 검증할 수 있다.

```
2026-07/26년 7월 배포 식단표/0. CK 2026년 7월 식단표(최종) (5).xlsx
  시트 2개: '7월식단표', '7월식단표 (2)'      ← (2)의 정체 미확인

2026-07/26년 7월 배포 식단표/키즈밀_식단표_26_07_v6.xlsx
  시트 8개: 1~5주차, 도시락·대체식 요청, 원별 참조표, 입력 가이드
  ★기준폼 v6 실물★ — v6 개선 9가지 작업 시 이것과 대조

2026-08/26년 8월 배포 식단표/0. CK 2026년 8월 식단표.xlsx
  시트 1개: '8월식단표'
```

7월에는 `7월 영양사 실제 엑셀 분석 → 기준폼 v6 강화 포인트.docx`도 있다.

## 파일명 규칙

```
26.07_키즈밀 튼튼식단_(07.01-07.31)(영통POLY).pptx
└연월┘              └───기간───┘└─원 약칭─┘
```

원 약칭이 파일명에 있어 `branch_profiles.short_code`와 짝지을 수 있다.

## ★함정 1★ 계열 폴더명이 달마다 다르다

수작업이라 그때그때 다르게 묶었다. 폴더명으로 계열을 판정하지 말 것.

```
7월   POLY, ECC   /  MB,알티오라,SLP  /  라이즈        /  기타원
8월   폴리,ECC    /  SLP,MB,알티      /  랜퍼스,라이즈  /  기타원
```

8월에 `랜퍼스`가 새로 묶였고 파일 수도 늘었다(7월 102 → 8월 117).

## ★함정 2★ 한 겹 더 들어가 있다

압축을 푼 폴더가 통째로 들어와서 경로가 한 단계 깊다.

```
2026-07/26년 7월 배포 식단표/...
2026-08/26년 8월 배포 식단표/...
```

## 엘란원은 JPG로도 나간다

7·8월 모두 `기타원/…(엘란)/` 아래에만 JPG 4장과 zip이 있다. `branch_profiles.file_format`의 `jpg` 옵션이 이 원이다. **JPG 변환(로드맵 ⑫) 작업 시 정답본이 여기 있다.**

## templates — 디자인 양식 9개

```
2026년 1월~7월 식단표.pptx
2026년 8월 식단표(여름방학O).pptx   8,405KB
2026년 8월 식단표(여름방학X).pptx     321KB   ★용량 차이 26배, 미확인★
```

방학 유무로 양식이 갈린다. `templates/page.tsx`의 "방학 구분" 필드, `template_resolver.py`의 선택 로직을 이것으로 검증할 수 있다.

## 6월·9월은 여기 없다

이미 Supabase Storage `diet-files` 버킷에 있고 `weekly_menus.pptx_url`에 URL이 박혀 있다. **다만 솔아의 컨테이너는 Supabase 도메인에 접근할 수 없다** (허용 목록에 없음 — 2026-09-08 실측, `Host not in allowlist`). 필요하면 유대표님이 내려받아 이 레포에 올리거나 채팅에 첨부해야 한다.

## 갱신 방법

파일을 추가·교체한 뒤 세 줄이면 된다.

```
git add .
git commit -m "설명"
git push
```

이 MANIFEST의 실측 수치도 그때 함께 고칠 것.
